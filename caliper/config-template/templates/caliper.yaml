caliper:
  fabric:
    gateway:
      usegateway: true
      localhost: false
      enabled: false
  logging:
    targets:
      console:
        options:
          level: info  # debug
      file:
        enabled: true
        options:
          # append mode
          flags: a
          # make the file readable/writable by anyone
          mode: 0666