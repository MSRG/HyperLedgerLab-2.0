test:
  name: basic-contract-benchmark
  description: test benchmark
  workers:
    number: 10
  rounds:
    - label: readAsset
      description: Read asset benchmark
      txNumber: 1500  
      rateControl:
        type: fixed-feedback-rate
        opts:
          tps: 1500
          # startingTps: 3000
          transactionLoad: 10
      workload:
        module: /caliper-workload/readAsset.js
        arguments:
          assets: 20
          contractId: asset-transfer-basic
